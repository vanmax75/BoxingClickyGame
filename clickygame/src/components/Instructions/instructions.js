import React from 'react';
import './instructions.css';

const Instructions = () => {
	return (
		<div id="instructions">
			<h5>Don't click the same picture twice!</h5>
		</div>
	);
}

export default Instructions;